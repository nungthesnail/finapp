# Web (Vue 3 + Vite)

Run in docker-compose via `npm run dev`.

Build check:

```bash
npm run build
```

The app covers stage 1 and stage 2 flows:
- auth by phone/password
- accounts and transactions
- income/expense categories and defaults
- recurring transactions
- budget plans
- analytics summary

